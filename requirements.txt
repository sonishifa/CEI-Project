# Core ML stack (used in Kaggle notebooks for training/evaluation)
torch>=2.0.0
torchvision>=0.15.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Image handling
Pillow>=10.0.0
opencv-python>=4.8.0

# Dashboard (local only — not needed on Kaggle)
streamlit>=1.28.0

# Utilities
tqdm>=4.66.0

# Bonus tasks
grad-cam>=1.4.8      # Bonus A - GradCAM
umap-learn>=0.5.5    # Bonus C - embedding visualization (t-SNE ships with scikit-learn)
